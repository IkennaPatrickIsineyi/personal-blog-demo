import { SxProps } from "@mui/material";

type PropKeys = {
    container: SxProps,
};

export const aboutPageStyle: PropKeys = {
    container: {
        backgroundColor: 'background.default',
        color: 'text.primary'
    }
}