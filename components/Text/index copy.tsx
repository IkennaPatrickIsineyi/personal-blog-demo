import { Typography } from "@mui/material";

type Props = {
    size: 'small' | 'tiny' | 'normal' | 'medium' | 'large' | 'extra'
}

export default function Text({ size }: Props) {
    return <Typography>

    </Typography>
}