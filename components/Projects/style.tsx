import { SxProps } from "@mui/material";

type PropKeys = {
    container: SxProps,
};

export const projectPageStyle: PropKeys = {
    container: {
        backgroundColor: 'background.default',
        color: 'text.primary'
    }
}