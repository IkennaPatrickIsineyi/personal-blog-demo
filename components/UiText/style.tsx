import { SxProps } from "@mui/material";

type PropKeys = {
    container: SxProps,
};

export const textStyle: PropKeys = {
    container: {
        backgroundColor: 'background.default',
        color: 'text.primary',
    },
}