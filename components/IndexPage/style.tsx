import { SxProps } from "@mui/material";

type PropKeys = {
    container: SxProps,
};

export const indexPageStyle: PropKeys = {
    container: {
        backgroundColor: 'background.default',
        color: 'text.primary'
    }
}